# ================================================================================
# GENESIS App — Environment Configuration
# ================================================================================

# ================================================================================
# MOBILE (Expo Public) — Must start with EXPO_PUBLIC_ prefix
# ================================================================================

# BFF URL — local development or Cloud Run endpoint
# Local: http://localhost:8000
# Cloud: https://[PROJECT_ID]-[HASH].run.app (after Cloud Run deployment)
EXPO_PUBLIC_BFF_URL=http://localhost:8000

# Supabase — for mobile direct queries (optional, bypasses BFF for some operations)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Vision API URL (camera scanner)
EXPO_PUBLIC_VISION_API_URL=http://localhost:8000/mobile/vision

# ElevenLabs (voice call audio pipeline)
EXPO_PUBLIC_ELEVENLABS_API_URL=https://api.elevenlabs.io
EXPO_PUBLIC_ELEVENLABS_AGENT_ID=your-agent-id-here

# ================================================================================
# BACKEND (BFF & Cloud Run) — Sensitive, NOT exposed to mobile
# ================================================================================

# Database
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Google Cloud & Gemini
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_API_KEY=your-google-api-key
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1

# Agent Engine (Vertex AI) — Sprint 5 deployment
AGENT_ENGINE_ENDPOINT=https://your-agent-engine-endpoint.googleapis.com

# Cache & Redis (Upstash) — optional for L2 distributed cache
UPSTASH_REDIS_URL=https://your-endpoint.upstash.io
UPSTASH_REDIS_TOKEN=your-token-here

# Voice API
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# JWT Signing Secret — for BFF auth
BFF_JWT_SECRET=your-secret-key-min-32-chars-for-hs256-algo

# ================================================================================
# BUILD & DEPLOYMENT
# ================================================================================

# EAS Project ID — set via `npx eas init` after cloning
EAS_PROJECT_ID=your-eas-project-id

# BFF Host & Port (used by local dev server)
BFF_URL=http://localhost:8000

# Build environment — development, staging, or production
ENVIRONMENT=development
